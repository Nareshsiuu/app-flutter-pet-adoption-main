name: pet_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  cached_network_image: ^3.1.0
  carousel_slider: ^4.0.0
  google_fonts: ^2.1.1
  flutter_svg: ^0.22.0
  glassmorphism_ui: ^0.2.0
  intl: ^0.19.0
  image_picker: ^1.1.2
  flutter_map: ^4.0.0
  geolocator: ^13.0.1
  firebase_auth: ^5.3.0
  firebase_core: ^3.5.0
  cloud_firestore: ^5.4.2
  firebase_storage: ^12.3.1
flutter_map: ^4.0.0
latlong2: ^0.8.1
geolocator: ^9.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.9.2"
flutter_icons:
    android: true
    ios: true
    image_path: "assets/icons/download.png"

flutter:
  uses-material-design: true
  
  assets:
    - assets/icons/
    - assets/images/
    - assets/pet_images/
    
